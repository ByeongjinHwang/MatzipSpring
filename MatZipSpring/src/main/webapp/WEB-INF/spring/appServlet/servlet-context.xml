<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/mvc"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd
		http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd">

	<!-- DispatcherServlet Context: defines this servlet's request-processing infrastructure -->
	
	<!-- Enables the Spring MVC @Controller programming model -->
	<!--  annotation : 맵핑 컨트롤러 역할-->
	<annotation-driven />

	<!-- Handles HTTP GET requests for /resources/** by efficiently serving up static resources in the ${webappRoot}/resources directory -->
	<!-- 리소스 맵핑 역할 -->
	<!-- 이름 줄일 수 있음 -->
	<!-- <resources mapping="/resources/**" location="/resources/" />  -->
	<resources mapping="/res/**" location="/resources/" />

	<!-- Resolves views selected for rendering by @Controllers to .jsp resources in the /WEB-INF/views directory -->
	<beans:bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<beans:property name="prefix" value="/WEB-INF/views/" />
		<beans:property name="suffix" value=".jsp" />
	</beans:bean>
	
	<!-- 역할 : component 및  annotation   -->
	<!--  annotation : 맵핑 컨트롤러 역할-->
	<context:component-scan base-package="com.koreait.matzip" />
	
	
	<!-- beans:property : setter 값을 넣음 -->
	<!--  CP(Connection Pool) : 미리 Connection을 많이 만들어서 사용한다  : 여러명이 사용할때 서버 퍼포먼스를 좋아지게 만듬 -->
	<!-- db연결 (히카리) -->
	<beans:bean id="hikariConfig" class="com.zaxxer.hikari.HikariConfig">
		<beans:property name="poolName" value="springHikariCP" />
		<!-- 커넥션이 유효한지 검사할 때 사용할 쿼리를 지정 -->
		<beans:property name="connectionTestQuery" value="SELECT 1 FROM DUAL" />
		<beans:property name="driverClassName" value="com.mysql.cj.jdbc.Driver" />
		<!--  데이터베이스에 대한 실제 커넥션의 최대 개수를 결정 이 수치를 넘으면  getConnection() 호출은 블록킹  -->
		<beans:property name="maximumPoolSize" value="10" />
		<!-- 5초안에 서버연결 못하면 에러뜸-->
		<beans:property name="idleTimeout" value="5000" />
		<!-- 접속정보 -->
		<beans:property name="jdbcUrl" value="jdbc:mysql://127.0.0.1:3306/matzip?useUnicode=true&characterEncoding=UTF-8" />
		<beans:property name="username" value="root" />
		<beans:property name="password" value="koreait2020" />
	</beans:bean>
	
	
	<!-- beans:constructor-arg : 생정자를 통해 값을 넣음 -->
	<beans:bean id="dataSource" class = "com.zaxxer.hikari.HikariDataSource" destory-method="close"> <!-- HDS 눌러서 컨트롤 + 스페이스 하면 자동완성 -->
		<beans:constructor-arg ref="hikariConfig" />
	</beans:bean>
